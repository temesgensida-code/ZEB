# For local dev with the Vite proxy, leave this empty (requests go to localhost:8000).
# For Netlify deployment, set this to your Render backend URL.
VITE_API_BASE_URL=https://<your-service>.onrender.com
